<h1>All Movies</h1>

<%= @movies.each do |movie| %>
<%= link_to image_tag(movie.image, :class => 'thumb'), movie %>

  <ul>
    <li><%= link_to movie.title, movie  %></li>
    <li><%= movie.genre %></li>
    <li><%= movie.rating  %></li>
  </ul>

<% end %>
